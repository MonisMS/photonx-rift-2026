# Google Gemini API Keys (free tier — ~30 req/day per key)
# Get free keys at https://aistudio.google.com/apikey
# Only GOOGLE_API_KEY_1 is required. Add more for rate limit rotation.
# The app tries all 4 keys × 2 models before falling back to OpenAI.

GOOGLE_API_KEY_1=your_gemini_key_here
GOOGLE_API_KEY_2=your_gemini_key_here
GOOGLE_API_KEY_3=your_gemini_key_here
GOOGLE_API_KEY_4=your_gemini_key_here

# OpenRouter API Key (free fallback — used after Gemini is exhausted)
# Get a free key at https://openrouter.ai/keys
# Provides free models: Llama 3.3 70B, Llama 3.1 8B, Mistral 7B. Optional.

OPENROUTER_API_KEY=your_openrouter_key_here

# OpenAI API Key (paid last resort — only used when Gemini + OpenRouter are exhausted)
# Get a key at https://platform.openai.com/api-keys
# Uses gpt-4o-mini (~$0.0007 per 6-drug run). Optional but recommended for production.

OPENAI_API_KEY=your_openai_key_here
